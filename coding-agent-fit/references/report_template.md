# 报告模板

输出简体中文。语气按技术文档：直接、清楚、有证据。

## 文风规则

### 判断段（各维度"判断"字段）

1. **从 Agent 体验写，不从扫描器视角写。** 写"Agent 走到这一步会怎样"，不写"我们检测到 X 缺失"。读者要的是体感，不是扫描结果清单。
2. **说清为什么是这档，不是相邻档。** 不只写"给 4 分"；写"有 X 所以不是 3，缺 Y 所以不是 5"。
3. **区分"未发现"和"不存在"。** probe / fetch / search 没看到 ≠ 服务没提供。没找到时写"本次评测未发现"，不写"缺失"或"没有"。

### 改进建议段（各维度"建议"字段 + "最该先改的 3 件事"）

1. **主语用站点或第三人称，评测方用"我们"。** 不预设读者身份——不用"你"，不用"如果你是 owner"。
2. **格式："我们看到的"→"改进方向"。** 前者陈述事实和对 Agent 接入的实际影响；后者给出站点可以做的事和衡量标准。
3. **面向使用本 skill 做评测的用户。** 报告是评测方写给使用者的交付物。改进建议是基于评测发现提出的优化方向——读者拿去推动站点改进、评估接入风险、比较多家服务，按自己角色决定怎么用。

### 双分数展示规则

Agent 得分是主角。基线 60 分大多数站点都能拿到大半，总分差距因此被压小，真正的区分度在 Agent 得分上——报告头和核心判断都要把"Agent 得分 y/40"放在显眼位置，横向比较服务时优先引用 Agent 得分。

---

```markdown
# Coding Agent 接入评测

**目标网站:** <URL>
**评测时间:** <date>
**网站类型:** <云服务 / API 平台 / 开发者工具 / Agent 工具 / 混合型>
**采用权重表:** <对应站点类型>

**基线得分:** <x>/60（传统开发者接入基础）
**Agent 得分:** <y>/40（Agent 友好增量<；触发耦合上限时注明：受基线 × 2/3 = z 封顶>）
**总分:** <score>/100 — 等级: <S/A/B/C/D/F>
**评测置信度:** <高 / 中 / 低> (worst-of 各维度置信度)

**Agent 接入把握:** <高 / 中 / 低>（基于阶段 4.5 实跑：<一句话引用实跑结果>）
**人工介入点:** <无明显人工介入 / 需要拿 key / 需要补 API 判断 / 需要人工排障 / 需要联系销售或支持>

## 核心判断

<直接说明这个服务交给 Claude Code / Cursor / Codex 接入时大概有多顺。先给两轨各自的结论——基线项是否完整、Agent 投入到什么程度——再说主要优势、最大卡点。>

## 接入路径

1. <Agent 如何找到入口。>
2. <Agent 如何读取文档并定位接入资料。>
3. <Agent 如何拿到鉴权、SDK、API 或工具信息。>
4. <Agent 如何生成并验证接入代码。>
5. <Agent 最可能卡住的一步。>

## 基线达标（60 分轨）

Σ适用项满扣 = <n>，Σ实际扣分 = <m>，基线得分 = 60 × (1 − m/n) = <x>

| # | 基线项 | 判定 | 扣分 | 证据 |
|---|--------|------|------|------|
| B1 | 文档公开可访问 | 完整/部分/缺失 | 0/x | <URL + T级> |
| B2 | API reference / 命令参考 | … | … | … |
| …  | （按 rubric.md 基线清单逐项，适用项全列） | | | |

**未验证基线项:** <无 / 列出项 + 原因（探测被拦 / 本次未覆盖），不计扣分但压低相关维度置信度>

## 接入实跑

**接入需求:** <用户给的或推断的任务，一句话>
**选定路径:** <REST / SDK / CLI / MCP / dashboard>
**选定理由:** <一句话>

| 步骤 | 结果 | 卡点 / 备注 |
|------|------|------------|
| 找入口 | ✓ / ✗ / 跳过 | <备注> |
| 读 quickstart | ✓ / ✗ | <备注> |
| 拿 key | ✓ / ⚠️ / ✗ | <备注> |
| 写调用 | ✓ / ✗ | <备注> |
| 实跑 | ✓ / ✗ / 跳过 | <备注> |
| 回查次数 | <N> | <说明回查了什么> |

**实跑总评:** <顺畅 / 少量卡点 / 多次卡顿 / 未跑通>
**实跑卡点映射:**
- 基线项: <触发的 B# 及判定变化>
- A4 摩擦: <触发的扣分项>
- 其他维度: <R 变化>

## Agent 轨评分概览（40 分池）

| 维度 | 权重 | 证据比 R | 维度档 | 置信 | 贡献 (R×权重×40) |
|------|------|---------|--------|------|-----------------|
| A1. Agent 入口与发现 | 15% | x.xx | x | 高/中/低 | x.xx |
| A2. 文档直接可读 | 30% | x.xx | x | 高/中/低 | x.xx |
| A3. Agent 工具 | 30% | x.xx | x | 高/中/低 | x.xx |
| A4. Agent 专属摩擦 | 15% | x.xx | x | 高/中/低 | x.xx |
| A5. 维护信号可跟踪 | 10% | x.xx | x | 高/中/低 | x.xx |
| **Agent 得分** | **100%** | — | — | **worst-of** | **y.yy** |

**耦合检查:** min(<Agent 得分>, <基线得分> × 2/3 = <上限>) = <Agent 实得分>（<受限时说明原因>）

## 各维度详情

### A1. Agent 入口与发现 (15%) — 档位: x/5

**证据比:** R = a/12 = 0.xx
**置信度:** 高/中/低 (strong_ratio=0.xx, covered_ratio=0.xx, dry_run_verified=true/false)
**证据:**
- T1 <URL>
- T2 <URL>

**判断:** <从 Agent 体验出发——说清为什么是这档而不是相邻档；区分"未发现"和"不存在">

**建议:** <我们看到的 → 改进方向；主语用站点，不预设读者身份>

---

### A2. 文档直接可读 (30%) — 档位: x/5

**证据比:** R = a/16 = 0.xx
**置信度:** 高/中/低 (strong_ratio=0.xx, covered_ratio=0.xx, dry_run_verified=true/false)
**证据:** <列出 URL 和等级>

**判断:** <从 Agent 体验出发——说清为什么是这档而不是相邻档；区分"未发现"和"不存在">

**建议:** <我们看到的 → 改进方向；主语用站点，不预设读者身份>

---

### A3. Agent 工具 (30%) — 档位: x/5

**证据比:** R = a/16 = 0.xx（生态级工具按 0.5 计的项注明）
**置信度:** 高/中/低 (strong_ratio=0.xx, covered_ratio=0.xx, dry_run_verified=true/false)
**证据:** <列出 URL 和等级>

**判断:** <从 Agent 体验出发——说清为什么是这档而不是相邻档；区分"未发现"和"不存在">

**建议:** <我们看到的 → 改进方向；主语用站点，不预设读者身份>

---

### A4. Agent 专属摩擦 (15%) — 档位: x/5

**置信度:** 高/中/低 (strong_ratio=0.xx, covered_ratio=0.xx, dry_run_verified=true/false)

**扣分拆解:**

| 扣分项 | 触发 | 分值 | 证据 |
|--------|------|------|------|
| <软 404 / SPA / 反爬 / llms.txt 或 .md 失效 / 有宣传无配置> | ✓ | x | <URL 或 dry_run 引用> |

**friction:** Σ = x / 10，R = 1 − x/10 = 0.xx
**最可能卡住的一步:** <按 rubric.md 挑选规则，引用实跑或扣分项证据>

**建议:** <我们看到的 → 改进方向；主语用站点，不预设读者身份>

---

### A5. 维护信号可跟踪 (10%) — 档位: x/5

**证据比:** R = a/6 = 0.xx
**置信度:** 高/中/低 (strong_ratio=0.xx, covered_ratio=0.xx, dry_run_verified=true/false)
**证据:** <列出 URL 和等级>

**判断:** <从 Agent 体验出发——说清为什么是这档而不是相邻档；区分"未发现"和"不存在">

**建议:** <我们看到的 → 改进方向；主语用站点，不预设读者身份>

## 最该先改的 3 件事

低置信度维度的建议优先排前。基线缺失项（若有）优先于 Agent 增量项。

1. **<改进标题>** (优先级: P0/P1/P2, 工作量: S/M/L, 预期提分: <基线 B# +x 分 / 维度 Ax +0.xx R ≈ +x.x 总分>)

   **我们看到的:** <客观事实——站点现状和对 Agent 接入的实际影响。主语是站点或具体资源，不预设读者身份。>

   **改进方向:** <站点可以做什么、做完后的衡量标准。用"站点新增…"/"CLI 文档补…"这类第三人称表述。>

2. **<改进标题>** (优先级: P0/P1/P2, 工作量: S/M/L, 预期提分: …)

   **我们看到的:** <…>

   **改进方向:** <…>

3. **<改进标题>** (优先级: P0/P1/P2, 工作量: S/M/L, 预期提分: …)

   **我们看到的:** <…>

   **改进方向:** <…>
```
